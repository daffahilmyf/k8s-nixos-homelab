apiVersion: apps/v1
kind: Deployment
metadata:
  name: traefik-gateway
  namespace: traefik
spec:
  template:
    spec:
      containers:
        - name: traefik
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 200m
              memory: 512Mi
