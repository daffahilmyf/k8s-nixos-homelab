apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins
  namespace: jenkins
spec:
  template:
    spec:
      containers:
        - name: jenkins
          resources:
            requests:
              cpu: "1"
              memory: "1.5Gi"
            limits:
              cpu: "2"
              memory: "2Gi"
