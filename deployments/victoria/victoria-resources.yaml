apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: victoria-metrics
spec:
  template:
    spec:
      containers:
        - name: victoria-metrics
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 200m
              memory: 512Mi
